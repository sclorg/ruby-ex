dev:
  build:
    frontend:
      templates:
      - manifest: manifests/build.yaml
  deployment:
    frontend:
      delete:
      - type: pvc
        name: frontend
      templates:
      - manifest: manifests/app.yaml
        parameters:
          ENV: test
          REPLICAS: 1
test:
  deployment:
    frontend:
      templates:
      - manifest: manifests/app.yaml
        parameters:
          ENV: test
          REPLICAS: 2
